'use strict';

/**
 * hero-section-statistic service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::hero-section-statistic.hero-section-statistic');
