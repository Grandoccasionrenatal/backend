'use strict';

/**
 * hero-section-statistic router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::hero-section-statistic.hero-section-statistic');
